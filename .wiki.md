# Project Summary
ScoutTalent is an AI-powered football talent marketplace that connects African youth players with global scouts. The platform facilitates player scouting, profile management, and enhances visibility for aspiring athletes through a microservices architecture.

# Project Module Description
The project consists of the following functional modules:
- **Auth Service**: Handles user registration, authentication, and role management.
- **Profile Service**: Manages player and scout profiles, including details, trust levels, and profile completion scoring.

# Directory Tree
```
scouting/
├── services/          # Microservices (Go)
│   ├── auth-service/          # Handles authentication
│   ├── profile-service/       # Manages player profiles
│   ├── media-service/         # In Progress
│   └── ...
├── pkg/              # Shared Go packages
├── web/              # Next.js frontend
├── helm/             # Helm charts
├── k8s/              # Kubernetes manifests
├── terraform/        # Infrastructure as Code
└── docs/             # Architecture documentation
```

# File Description Inventory
- **Makefile**: Contains commands for building, testing, and managing services.
- **docker-compose.yml**: Defines the services for the application and their configurations.
- **init-databases.sql**: Script to create databases for each service.
- **QUICKSTART.md**: Quick start guide for setting up and running the project.
- **README.md**: Overview of the project, architecture, and services.
- **scripts/test-api.sh**: Script to automate API testing.

# Technology Stack
- **Backend**: Go 1.22+ with Gin framework
- **Databases**: PostgreSQL 18.1
- **Cache**: Redis 8.4
- **Message Queue**: NATS JetStream
- **Containerization**: Docker
- **Orchestration**: Kubernetes (AKS)

# Usage
### Prerequisites
- Go 1.22+
- Docker & Docker Compose
- Node.js 20+
- golang-migrate

### Quick Start
```bash
# 1. Start all services
make dev

# 2. Run database migrations
make migrate-up

# 3. Test the API
./scripts/test-api.sh
```

### Manual Testing
#### 1. Register a Player
```bash
curl -X POST http://localhost:8080/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "player@example.com",
    "password": "password123",
    "role": "player"
  }'
```
#### 2. Login
```bash
curl -X POST http://localhost:8080/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "player@example.com",
    "password": "password123"
  }'
```
Save the `access_token` from the response.

#### 3. Create Profile
```bash
export TOKEN="your-access-token-here"

curl -X POST http://localhost:8081/api/v1/profiles \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "display_name": "John Doe",
    "bio": "Aspiring professional footballer",
    "location_country": "Nigeria",
    "location_city": "Lagos"
  }'
```

#### 4. Add Player Details
```bash
curl -X POST http://localhost:8081/api/v1/profiles/{profile_id}/player-details \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "position": "forward",
    "height_cm": 180,
    "weight_kg": 75,
    "preferred_foot": "right",
    "current_team": "Lagos FC"
  }'
```

#### 5. Get Player Profile
```bash
curl http://localhost:8081/api/v1/profiles/{profile_id}/player \
  -H "Authorization: Bearer $TOKEN"
```
